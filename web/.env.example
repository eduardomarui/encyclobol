# Ranking (Supabase). Copie para .env.local e preencha com os dados do seu projeto.
# A anon key é pública (vai no bundle do front) — pode usar sem medo.
# Sem essas variáveis, o app funciona normal e o ranking fica "Em breve".
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
